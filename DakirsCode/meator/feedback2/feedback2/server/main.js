//this is server side code
import { Meteor } from 'meteor/meteor';
//here we pull in feedback.js from our api folder
// which is the file that has our database stuff in it
import Feedback from "../imports/api/feedback"

Meteor.startup(() => {
  
});
